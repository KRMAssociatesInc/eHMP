package gov.va.cpe.vpr;

public class UidLink {
	String uid; // Reference some other thing.
	/*
	 * Object.getProperties() will return any other stuff that's associated with this link.
	 * Ex: A procedure result (document UID) has an interpretation associated with it;
	 * The interpretation would go into this objet's properties (interpretation: "whatever")
	 */
}
