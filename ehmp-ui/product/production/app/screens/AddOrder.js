define(function() {
    var addOrderConfig = {
        id: "add-order",
        contentRegionLayout: "gridOne",
        appletHeader: "patient",
        applets: [{
            id: "addOrder",
            title: "add-order",
            region: "center"
        }]
    };

    return addOrderConfig;
});
