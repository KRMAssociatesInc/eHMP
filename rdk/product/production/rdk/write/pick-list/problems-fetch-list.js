/*jslint node: true */
'use strict';
var VistaJS = require('../core/VistaJS');

module.exports.getProblemsLex = function(log, configuration, callback) {
    /*VistaJS.callRpc(logger, configuration, 'ORQQPL4 LEX', '', function(err, result) {
        if (err)
            res.status(500).send(err.message);
        else
            res.status(200).send(result);
    });*/
};
